
## Setup

1. Clone the repository: `git clone https://github.com/toeyldev/enterprise-appointment-system.git`
2. `cd enterprise-appointment-system`


## Run the application
- From the terminal: `./mvnw spring-boot:run`

The API will start at http://localhost:8080/
